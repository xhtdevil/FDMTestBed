#!/bin/bash

sudo ovs-vsctl set bridge s1 stp-enable=true
sudo ovs-vsctl set bridge s2 stp-enable=true
sudo ovs-vsctl set bridge s3 stp-enable=true
sudo ovs-vsctl set bridge s4 stp-enable=true
sudo ovs-vsctl set bridge s5 stp-enable=true
sudo ovs-vsctl set bridge s6 stp-enable=true
sudo ovs-vsctl set bridge s7 stp-enable=true
sudo ovs-vsctl set bridge s8 stp-enable=true
sudo ovs-vsctl set bridge s9 stp-enable=true
sudo ovs-vsctl set bridge s10 stp-enable=true
sudo ovs-vsctl set bridge s11 stp-enable=true
sudo ovs-vsctl set bridge s12 stp-enable=true

